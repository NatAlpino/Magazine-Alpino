import styled from "styled-components";
import Grid from "@mui/material/Grid";

const styledButtonDetails = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
  }
`;

export default styledButtonDetails;
