import styled from "styled-components";
import Grid from "@mui/material/Grid";

const StyledButtonFavorite = styled(Grid)`
  && {
    display: flex;
    justify-content: end;
    margin: 20px;
  }
`;

export default StyledButtonFavorite;
